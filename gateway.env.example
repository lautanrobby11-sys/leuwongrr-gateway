# Non-canonical legacy template — do not copy values from this file.
#
# The single source of truth for the runtime environment contract is
# `.env.example` (installed as /opt/leuwongrr-gateway/config/gateway.env with
# root:root mode 0600). An older version of this file carried example values
# that looked real; treat it as unsafe and use .env.example instead.
